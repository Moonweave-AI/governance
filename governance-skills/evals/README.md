# Evals

Run:

```bash
node bin/moonweave-skills.mjs eval-static
```

Static evals check routing heuristics and skill frontmatter. Real model evals should run the paired experiments defined in `docs/EVALUATION.md` on each platform, recording model, platform, version, randomness, tool permissions, and results.
