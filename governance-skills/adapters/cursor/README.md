# cursor adapter

Generated by:

```bash
moonweave-skills install --agents cursor --root /path/to/project
```

The adapter installs standard skills plus the smallest platform-specific rules and command wrappers. It does not fork governance semantics.
