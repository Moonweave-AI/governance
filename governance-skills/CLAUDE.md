# Claude Code Instructions

Follow `AGENTS.md`. Use the focused skill matching the task; use `moonweave-governance-router` when the correct workflow or risk level is unclear. Generated native plugin output must be rebuilt with `npm run build:adapters`, not edited by hand.
